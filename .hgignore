syntax: glob

# Scripts used for backing up stuff like the database etc (would be specific for each computer)
backup.bat
backup.sh

# Backup files left behind by the Emacs editor.
*~

# Lock files used by the Emacs editor.
# Notice that the "#" character is quoted with a backslash.
# This prevents it from being interpreted as starting a comment.
.\#*

# Temporary files used by the vim editor.
.*.swp

# A hidden file created by the Mac OS X Finder.
.DS_Store

# switch to regexp syntax.
syntax: regexp

# ignore all output/ directories
(^|/)output/

# ignore all books in books/ directories except 613839
(^|/)books/(?!613839)

# ignore temporary files, logs and profile area
(^|/)profiles/
(^|/)player/logs/
(^|/)player/prepare/temp/